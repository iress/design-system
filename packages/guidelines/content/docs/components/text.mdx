---
title: Text
description: "The IressText component allows you to set typographic styles either on one element, or a block on HTML elements."
component: "Text/Text.tsx"
propsType: "IressTextProps"
---

## Usage

### The `element` prop

With the `element` prop you can select which HTML element you would like the text component to render as.

It renders as a `div` by default, but can also be set to any standard typography element.

[Open Element in Storybook →](https://main--691abcc79dfa560a36d0a74f.chromatic.com/?path=/story/components-text--element)

### The `textStyle` prop

The `textStyle` prop (previously `variant`) allows you alter the default styling of the element that it selected.

For example, in order to maintain the semantic structure of headings, you may need to style a `h2` element like a `h4`. Or you may want to style your heading using one of our display text formats.

[Open Variant in Storybook →](https://main--691abcc79dfa560a36d0a74f.chromatic.com/?path=/story/components-text--variant)

### The `color` prop

The `color` prop (previously `mode`) can be used to set the colour of the text to these predefined mode colours: Body, Muted, Primary, Info, Success, Warning, Danger, Positive and Negative.

[Open Mode in Storybook →](https://main--691abcc79dfa560a36d0a74f.chromatic.com/?path=/story/components-text--mode)

### The `textAlign` prop

The `textAlign` prop (previously `align`) can be used to set the text's alignment.

[Open Align in Storybook →](https://main--691abcc79dfa560a36d0a74f.chromatic.com/?path=/story/components-text--align)

## Behaviour

### Block of typographic content

If you just need to style a block of typography content, you can just wrap the entire block of HTML with the text component.

[Open TypographicBlock in Storybook →](https://main--691abcc79dfa560a36d0a74f.chromatic.com/?path=/story/components-text--typographic-block)

## Props

<auto-type-table path="../../../../../packages/components/src/components/Text/Text.tsx" name="IressTextProps" />
