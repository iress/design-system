import { Meta } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './Container.stories';
import { ComponentOverview } from '@iress-oss/ids-storybook-config';

<Meta of={ComponentStories} />

# Container

<ComponentOverview
  description={
    <>
      <code>IressContainer</code> provides a means to center and horizontally
      pad your site’s contents.
    </>
  }
  of={ComponentStories.Container}
  meta={ComponentStories}
/>

## Behaviour

The `IressContainer` snaps to fixed max widths at different breakpoints. Those max widths are as follows:

<ComponentExample of={ComponentStories.BreakpointTable} />

## Examples

### Fluid

If you want the `IressContainer` to fill its containing element, you can set the `fluid` prop. Resize the example below to see how the IressContainer is 100% for all screen sizes.

<ComponentExample of={ComponentStories.Fluid} />
