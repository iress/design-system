import { Meta } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './Image.stories';
import {
  ComponentExample,
  ComponentOverview,
} from '@iress-oss/ids-storybook-config';

<Meta of={ComponentStories} />

# Image

<ComponentOverview
  description={
    <>
      <code>IressImage</code> renders an image that resizes based on the user's
      screen width. The component accepts standard image attributes like `src`
      and `alt`.
    </>
  }
  of={ComponentStories.Default}
  meta={ComponentStories}
/>

## Examples

### MaxWidth

`maxWidth` accepts formats like `70 (numbers)` `250px (pixels)` `50% (percentages)` `undefined (fall back to default 100%)`, allowing you to display the image smaller than its original size, yet still responsive on smaller screen sizes.

**Note:** Please consider not using this prop, but rather exporting images at the largest size required for your application (the maximum width).

<ComponentExample of={ComponentStories.MaxWidth} />
