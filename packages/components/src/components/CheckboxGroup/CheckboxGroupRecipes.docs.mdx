import { Meta } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './CheckboxGroupRecipes.stories';
import { ComponentExample } from '@iress-oss/ids-storybook-config';

<Meta of={ComponentStories} />

# Recipes

## Checkboxes inside an `IressTable`

You can use `IressCheckboxGroup` and `IressTable` to create a table with checkboxes, allowing the users to select multiple rows.

<ComponentExample of={ComponentStories.Table} meta={ComponentStories} />
