import { Meta } from '@storybook/addon-docs/blocks';
import * as SkeletonRecipesStories from './SkeletonRecipes.stories';
import { ComponentExample } from '@iress-oss/ids-storybook-config';

<Meta of={SkeletonRecipesStories} />

# Recipes

## `IressCard`

A common use case of the skeleton component is within a card component. Below are some examples of how you might use `IressSkeleton` within `IressCard`.

<ComponentExample of={SkeletonRecipesStories.Card} />
