import { Meta } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './Readonly.stories';
import {
  ComponentExample,
  ComponentOverview,
} from '@iress-oss/ids-storybook-config';

<Meta of={ComponentStories} />

# Readonly

<ComponentOverview
  description="Readonly displays a value that cannot be edited by the user. It renders a hidden input field to ensure the value is submitted with the form."
  of={ComponentStories.Default}
  meta={ComponentStories}
/>

## Examples

### Rich Content

You can pass in stylised content that represents the value by using the `children` prop. If no `children` is provided, it will display the `value` directly.

<ComponentExample of={ComponentStories.RichContent} />

### Inline

You can make the prepend/append element closer to the input content using the `inline` prop.

<ComponentExample of={ComponentStories.InlineStyle} />

### Actions

The `actions` prop allows you to add buttons next to the readonly field. These can be used to trigger actions related to the displayed value, such as copying it to the clipboard or opening an edit dialog.

<ComponentExample of={ComponentStories.Actions} />

### Locked

Use `variant="locked"` to indicate the value is read-only due to permissions.
This applies disabled-like styling (greyed out, `not-allowed` cursor) while
keeping the underlying input accessible to screen readers — unlike a truly
disabled input, which is excluded from the tab order and often ignored by
assistive technology.

When form controls such as `IressInput` or `IressSelect` are rendered with
`readOnly="locked"`, they automatically pass the locked variant through to
`IressReadonly`.

<ComponentExample of={ComponentStories.Locked} />
